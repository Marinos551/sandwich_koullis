
import 'package:flutter_test/flutter_test.dart';

void main() {
  // Dummy test to ensure the file is included in test coverage.
  test('cart_item_row.dart loads', () {
    expect(1, equals(1));
  });
}
